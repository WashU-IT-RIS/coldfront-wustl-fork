### Quick Start

1. Connect to Compute2 Login node
1. `sudo bash` to become root in the current working directory
1. Load any relevant modules include `slurm`
1. Execute the job one of two ways:
    1. Launch an interactive job with `srun.sh` to run the code
    1. Run the code as an array using `sbatch.sh` to parallelize execution